//page 41
// CALLBACKS
  // callbacks are functions that I pass as argument to be invoked when the callee(invocator -_-) job
  // has been done

  const setTimeoutCallback = function () {
      console.log('ta da')
  }
  setTimeout(setTimeoutCallback, 10);
