//page 40

  //Currying is the process of transforming a function that takes multiple arguments
  //into a chain of functions that each call wont take no more than one argument
  //sample
    //add(1,2,3) would become add(1)(2)(3) as the first call returns a function that returns a function and so on
